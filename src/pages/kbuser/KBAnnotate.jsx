import React from 'react';
import PlaceholderPage from '../placeholder/PlaceholderPage';
export default function KBAnnotate() {
  return <PlaceholderPage title="Annotate" description="Add annotations and tags to knowledge base entries." />;
}